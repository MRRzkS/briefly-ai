# Briefly AI — local environment
# Create .env.local and fill these values.

OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemini-2.5-flash

# Used only for provider attribution headers.
NEXT_PUBLIC_APP_URL=http://localhost:3000
